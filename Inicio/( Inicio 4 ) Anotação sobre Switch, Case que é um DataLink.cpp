switch (variável_a_ser_testada) 
// é tipo um datalink onde em cima ele irá testar uma determinada variavel e abaixo nos case ele irá testar se ela é o valor
// obs : que o case ele testa apenas se ela é um valor ou seja se ela é 1 se ela é A se ela é Vermelho se ela é Verdadeiro e etc. ou é ou não é 
{

  case valor1:

    // Comandos a serem executados.

    break; // Para o comando tipo return em lua

  case valor2:

    // Comandos a serem executados.

    break;

  case valorN:

    // Comandos a serem executados.

    break;

  default: // seria o caso contrario ou após o teste de todos os casos se nada der certo ele vai aplicar o default que é tipo um else de tudo

    // Comandos a serem executados caso a variável testada não seja

    // igual a nenhum dos valores testados nos “cases”.

}
